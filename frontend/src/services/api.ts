// src/services/api.ts

// This function handles the connection. 
// You can import this function into any login/signup page.

export const connectToBackend = async (actionType: string, data: any) => {
    console.log(`[Backend Service] Sending ${actionType}`, data);

    /* ------------------------------------------------
       PASTE YOUR BACKEND CODE HERE
       ------------------------------------------------
    */
    
    
    alert(`Data sent for ${actionType}! Check console for details.`);
};